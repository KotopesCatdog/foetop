git config --global user.name "KotopesCatdog"
git config --global user.email "163532389+KotopesCatdog@users.noreply.github.com"



cd C:\foetop

git status          # покажет, есть ли файлы для коммита

git add .           # добавляем все файлы

git status          # проверяем, что файлы добавились (зеленые)

git commit -m "Первая загрузка всех файлов сайта"

git branch          # должна показаться ветка (master или main)

git push -u origin master --force   # или main, в зависимости от вывода git branch