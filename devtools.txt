// === ПОЛНЫЙ ЭКСПОРТ ВСЕХ ДАННЫХ ===
const fullDB = {};

for (const [id, b] of Object.entries(MainParser.CityEntities)) {
    // Извлекаем ВСЕ здания (убрана фильтрация по префиксу)
    // if (!id.startsWith('W_')) continue;
    
    fullDB[id] = b;
}

// Копируем результат
copy(JSON.stringify(fullDB, null, 2));

// Статистика
console.log('='.repeat(70));
console.log('📦 ПОЛНЫЙ ЭКСПОРТ CITY ENTITIES');
console.log('='.repeat(70));
console.log(`✅ Всего объектов: ${Object.keys(fullDB).length}`);
console.log(`💾 Размер JSON: ${(JSON.stringify(fullDB).length / 1024 / 1024).toFixed(2)} MB`);
console.log('📋 Данные скопированы в буфер обмена!');
console.log('='.repeat(70));

// Пример структуры первого объекта
console.log('\n🔍 ПРИМЕР СТРУКТУРЫ (первый объект):');
const firstKey = Object.keys(fullDB)[0];
console.log(`ID: ${firstKey}`);
console.log('Ключи:', Object.keys(fullDB[firstKey]));